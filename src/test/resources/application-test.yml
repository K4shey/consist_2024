spring:
  application:
    name: task3
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
  flyway:
    url:
    user:
    password:
    locations: classpath:migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
  port: 8090